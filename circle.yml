machine:
  node:
    version: 6.9.1
general:
  branches:
    only:
      - master
dependencies:
  override:
    - npm install
    - npm install --only=dev
test:
  override:
    - npm run test
    - npm run check-coverage
    - npm run build